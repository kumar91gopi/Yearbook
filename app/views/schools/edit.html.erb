<div class="row"> 
  <div class="col s6 offset-s3" >
    <div class="card-panel hoverable">
        <h3 class= "center-align">Editing School</h3>

        <%= render 'form' %>
        
        <%= link_to 'Show', @school %> |
        <%= link_to 'Back', schools_path %>
        <%= button_to "Delete", school_path(@school), data: { confirm: "Are you sure?" }, method: :delete %>
    </div>
  </div>
</div>